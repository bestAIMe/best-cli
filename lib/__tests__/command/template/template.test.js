// const t = require('../../../command/template');
// const tempalte = require('../../../command/template/template');

describe('template', () => {
  // beforeAll(() => {
  //   t.initTemplates();
  // });

  it('generate tempalte', () => {
    // const name = 'lib-ts-template';
    // const url = 'git@gitlab.huobiapps.com:templates/lib-ts-template.git';

    // tempalte.generate(name, url);

    expect(1).toBe(1);
  });
});
